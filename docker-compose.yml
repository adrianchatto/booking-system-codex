services:
  bookright:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://bookright:${SERVICE_PASSWORD_POSTGRES}@postgres:5432/bookright
      NEXTAUTH_SECRET: ${SERVICE_PASSWORD_NEXTAUTH}
      NEXTAUTH_URL: https://bookingcodex.chattoweb.com
      NEXT_PUBLIC_ROOT_DOMAIN: bookingcodex.chattoweb.com
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      SUPER_ADMIN_EMAIL: ${SUPER_ADMIN_EMAIL:-superadmin@platform.com}
      SUPER_ADMIN_PASSWORD: ${SUPER_ADMIN_PASSWORD:-SuperAdmin2024!}
      SUPER_ADMIN_NAME: ${SUPER_ADMIN_NAME:-Platform Admin}
      NODE_ENV: production
    ports:
      - "3000"
    command: sh -c "npx prisma db push && npm run db:seed && node server.js"

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: bookright
      POSTGRES_USER: bookright
      POSTGRES_PASSWORD: ${SERVICE_PASSWORD_POSTGRES}
    volumes:
      - bookright_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bookright -d bookright"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  bookright_postgres_data:
